<Page x:Class="UCC.View.PageProfileDoctor"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d" 
      d:DesignHeight="600" d:DesignWidth="800"
      Title="Профиль врача">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Заголовок -->
        <TextBlock Text="Профиль врача" 
                   FontSize="20" FontWeight="Bold" 
                   Grid.Row="0" Margin="0,0,0,15"/>

        <!-- Данные врача -->
        <Border Grid.Row="1" BorderBrush="#CBD5E0" BorderThickness="1" CornerRadius="5" Padding="20" Background="#F8FAFC">
            <StackPanel Margin="10">
                <StackPanel Orientation="Horizontal" Margin="10">
                    <TextBlock Text="ФИО:" FontWeight="Bold" Width="120"/>
                    <TextBox x:Name="TxtFullName" Width="300" IsReadOnly="True"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal" Margin="10">
                    <TextBlock Text="Специальность:" FontWeight="Bold" Width="120"/>
                    <TextBox x:Name="TxtSpecialty" Width="300" IsReadOnly="True"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal" Margin="10">
                    <TextBlock Text="Участок:" FontWeight="Bold" Width="120"/>
                    <TextBox x:Name="TxtDistrict" Width="300" IsReadOnly="True"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal" Margin="10">
                    <TextBlock Text="Кабинет:" FontWeight="Bold" Width="120"/>
                    <TextBox x:Name="TxtCabinet" Width="100" IsReadOnly="True"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                    <TextBlock Text="Email:" FontWeight="Bold" Width="120"/>
                    <TextBox x:Name="TxtEmail" Width="300" IsReadOnly="True"/>
                </StackPanel>
            </StackPanel>
        </Border>

        <!-- Кнопки -->
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Grid.Row="2" Margin="0,20,0,0">
            <Button x:Name="BtnEdit" Content="Редактировать" Width="120" Click="BtnEdit_Click" Margin="0,0,10,0"/>
            <Button Content="Назад" Width="80" Click="BtnBack_Click"/>
        </StackPanel>

    </Grid>
</Page>
