<Window x:Class="UCC.View.WindowRefferalAdd"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Выдать направление" Height="400" Width="600"
        WindowStartupLocation="CenterOwner">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Новое направление" FontSize="18" FontWeight="Bold" Grid.Row="0" Margin="0,0,0,15"/>

        <!-- Выбор пациента -->
        <StackPanel Grid.Row="1" Margin="0,0,0,15">
            <TextBlock Text="Пациент:" FontWeight="Bold" Margin="0,0,0,5"/>
            <ComboBox x:Name="CmbPatients" DisplayMemberPath="FullName" SelectedValuePath="PatientId" Height="28"/>
        </StackPanel>

        <!-- Тип направления -->
        <StackPanel Grid.Row="2" Margin="0,0,0,15">
            <TextBlock Text="Тип направления:" FontWeight="Bold" Margin="0,0,0,5"/>
            <ComboBox x:Name="CmbReferralType" Height="28">
                <ComboBoxItem Content="Анализы"/>
                <ComboBoxItem Content="Рентген"/>
                <ComboBoxItem Content="УЗИ"/>
                <ComboBoxItem Content="КТ"/>
                <ComboBoxItem Content="МРТ"/>
                <ComboBoxItem Content="Консультация специалиста"/>
            </ComboBox>
        </StackPanel>

        <!-- Цель / Комментарий -->
        <StackPanel Grid.Row="3" Margin="0,0,0,15">
            <TextBlock Text="Цель / Комментарий:" FontWeight="Bold" Margin="0,0,0,5"/>
            <TextBox x:Name="TxtPurpose" AcceptsReturn="True" Height="60" TextWrapping="Wrap"/>
        </StackPanel>

        <!-- Кнопки -->
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Grid.Row="4">
            <Button Content="Отмена" Width="80" IsCancel="True" Margin="0,0,10,0"/>
            <Button Content="Создать направление" Width="140" Click="BtnCreate_Click" Background="#3182CE" Foreground="White"/>
        </StackPanel>

    </Grid>
</Window>