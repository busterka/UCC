<Page x:Class="UCC.View.PageAddCardDiagnose"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d" 
      d:DesignHeight="700" d:DesignWidth="900"
      Title="Добавление данных в карту">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Заголовок -->
        <TextBlock Text="Новый приём пациента" 
                   FontSize="20" FontWeight="Bold" 
                   Grid.Row="0" Margin="0,0,0,15"/>

        <!-- Пациент -->
        <StackPanel Orientation="Horizontal" Grid.Row="1" Margin="0,0,0,15">
            <TextBlock Text="Пациент:" FontWeight="Bold" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <TextBlock x:Name="TxtPatientName" Text="— не выбран —" FontSize="14"/>
        </StackPanel>

        <!-- Диагноз -->
        <StackPanel Grid.Row="2" Margin="0,0,0,15">
            <TextBlock Text="Диагноз (МКБ-10):" FontWeight="Bold" Margin="0,0,0,5"/>
            <TextBox x:Name="TxtDiagnosis" Text="Например: I10 – Гипертония" Height="30" Width="400"/>
        </StackPanel>

        <!-- Жалобы -->
        <StackPanel Grid.Row="3" Margin="0,0,0,15">
            <TextBlock Text="Жалобы пациента:" FontWeight="Bold" Margin="0,0,0,5"/>
            <TextBox x:Name="TxtComplaints" AcceptsReturn="True" Height="80" TextWrapping="Wrap"/>
        </StackPanel>

        <!-- Осмотр -->
        <StackPanel Grid.Row="4" Margin="0,0,0,15">
            <TextBlock Text="Осмотр / Симптомы:" FontWeight="Bold" Margin="0,0,0,5"/>
            <TextBox x:Name="TxtExamination" AcceptsReturn="True" Height="80" TextWrapping="Wrap"/>
        </StackPanel>

        <!-- Назначения -->
        <StackPanel Grid.Row="5" Margin="0,0,0,15">
            <TextBlock Text="Назначения и рекомендации:" FontWeight="Bold" Margin="0,0,0,5"/>
            <TextBox x:Name="TxtPrescriptions" AcceptsReturn="True" Height="80" TextWrapping="Wrap"/>
        </StackPanel>

        <!-- Доп. действия -->
        <StackPanel Orientation="Horizontal" Grid.Row="6" Margin="0,0,0,20">
            <Button Content="Выдать рецепт" Width="120" Margin="0,0,10,0" Click="BtnAddPrescription_Click"/>
            <Button Content="Выдать направление" Width="140" Click="BtnAddReferral_Click"/>
        </StackPanel>

        <!-- Кнопки -->
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Grid.Row="8">
            <Button Content="Отмена" Width="100" Margin="0,0,10,0" Click="BtnCancel_Click"/>
            <Button Content="Сохранить и завершить" Width="160" 
                    Background="#2B6CB0" Foreground="White" 
                    Click="BtnSaveAndFinish_Click"/>
        </StackPanel>

    </Grid>
</Page>