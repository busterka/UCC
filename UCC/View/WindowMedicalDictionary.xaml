<Window x:Class="UCC.View.WindowMedicalDictionary"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Медицинские справочники" Height="600" Width="900"
        WindowStartupLocation="CenterOwner">

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TabControl x:Name="TabControl" Grid.Row="1">

            <!-- Диагнозы -->
            <TabItem Header="Диагнозы">
                <Grid Margin="10">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBox x:Name="TxtSearchDiagnosis" 
                             TextChanged="TxtSearchDiagnosis_TextChanged"
                             Text="Поиск по коду или названию..." 
                             Margin="0,0,0,10"/>

                    <DataGrid x:Name="DgDiagnoses" Grid.Row="1" 
                              AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Код" Binding="{Binding Code}" Width="80"/>
                            <DataGridTextColumn Header="Название" Binding="{Binding Name}" Width="*"/>
                        </DataGrid.Columns>
                    </DataGrid>

                    <StackPanel Orientation="Horizontal" Grid.Row="2" HorizontalAlignment="Right" Margin="0,10,0,0">
                        <Button Content="Добавить" Width="90" Click="BtnAddDiagnosis_Click" Margin="0,0,10,0"/>
                        <Button Content="Закрыть" Width="80" IsCancel="True"/>
                    </StackPanel>
                </Grid>
            </TabItem>

            <!-- Лекарства -->
            <TabItem Header="Лекарства">
                <Grid Margin="10">
                    <DataGrid x:Name="DgMedications" AutoGenerateColumns="False" IsReadOnly="True">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="ID" Binding="{Binding MedicationId}" Width="50"/>
                            <DataGridTextColumn Header="Название препарата" Binding="{Binding MedicationName}" Width="*"/>
                            <DataGridTextColumn Header="Дозировка" Binding="{Binding StandardDosage}" Width="120"/>
                            <DataGridTextColumn Header="Инструкция" Binding="{Binding GeneralInstructions}" Width="2*"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </TabItem>

            <!-- Участки -->
            <TabItem Header="Участки">
                <Grid Margin="10">
                    <DataGrid x:Name="DgDepartments" AutoGenerateColumns="False" IsReadOnly="True">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Название" Binding="{Binding DepartmentName}" Width="*"/>
                            <DataGridTextColumn Header="Кабинет" Binding="{Binding Cabinet}" Width="150"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </TabItem>

        </TabControl>
    </Grid>
</Window>